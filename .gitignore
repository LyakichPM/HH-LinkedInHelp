# ============================================================
# SECRETS & PERSONAL DATA — NEVER COMMIT
# ============================================================
config.local.json
config/config.local.json
hh_cookies.json
*_cookies.json

# Browser profile — contains live session cookies (LinkedIn/hh.ru). Committing
# this = handing out account access. Keep it OUTSIDE the repo entirely.
chrome_profile/
**/chrome_profile/
*.sqlite
Cookies
Cookies-journal

# LinkedIn / hh.ru scrape dumps & debug artifacts (may embed personal data)
li_*
# ...but the LinkedIn automation SOURCE lives in li-tools/ — keep the code,
# the li_* rule above is meant for scrape/debug output files, not scripts.
!li-tools/
!li-tools/*.py
cover_*.html
*_debug.html
*_profile.html
*_profile*.json

# Ad-hoc debug scripts — historically hardcoded the bot token. Keep them local.
check_*.py
get_chat_id*.py
fetch_tg*.py
send_tg*.py
send_cover*.py
tg_monitor.py
tg_ack*.py
tg_*_result.py

# Telegram inbox queue
tg-bridge/tg_inbox.jsonl
tg_inbox.jsonl
tg_offset_poll.txt
tg-bridge/tg_group_state.json

# HR-outreach side experiment — contacts, outreach logs, login QR: personal data
hr-outreach/

# Binaries / large files
*.exe

# Debug / temporary files
*_results.json
*_results.txt
*_stderr.txt
*_stdout.txt
*_check_results.json
*_check_results.txt
*_fix_results.json
*_fix_results.txt
*_apply_results.json
*_apply_results.txt
*_debug.png
*_before.png
*_after.png
*_filled.png
*_result.png
chat_*.png
after_*.png
apply_*.png
li_*.png

# Cover letters — contain personal data (phone, salary expectations, name)
cover_*.txt
cover_letters/

# Generated marketing artifacts (local only)
linkedin_post_image.*

# Temporary scripts output
*.jsonl
*.log

# Python
__pycache__/
*.pyc
*.egg-info/
dist/
build/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Environment
.env
venv/
.venv/
li_profile/
